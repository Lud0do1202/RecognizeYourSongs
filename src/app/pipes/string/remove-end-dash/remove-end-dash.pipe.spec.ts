import { RemoveEndDashPipe } from './remove-end-dash.pipe';

describe('RemoveEndDashPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveEndDashPipe();
    expect(pipe).toBeTruthy();
  });
});
