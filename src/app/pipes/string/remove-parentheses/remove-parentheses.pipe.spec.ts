import { RemoveParenthesesPipe } from './remove-parentheses.pipe';

describe('RemoveParenthesesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveParenthesesPipe();
    expect(pipe).toBeTruthy();
  });
});
