import { RemovePunctuationsPipe } from './remove-punctuations.pipe';

describe('RemovePunctuationsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemovePunctuationsPipe();
    expect(pipe).toBeTruthy();
  });
});
